<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Admin - Pass/Fail Candidates</title>
</head>
<body>
    <%- include('layout/navbar'); -%>
    <div class="container">
        <h1 class="my-4">Pass/Fail Candidates</h1>

        <% if (candidates.length > 0) { %>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>License Number</th>
                        <th>Test Type</th>
                        <th>Status</th>
                        <th>Comments</th>
                    </tr>
                </thead>
                <tbody>
                    <% candidates.forEach(candidate => { %>
                        <tr>
                            <td><%= candidate.bookedBy.firstName %> <%= candidate.bookedBy.lastName %></td>
                            <td><%= candidate.bookedBy.licenseNumber %></td>
                            <td><%= candidate.testType %></td>
                            <td><%= candidate.status %></td>
                            <td><%= candidate.comments %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>No candidates found with Pass or Fail status.</p>
        <% } %>
    </div>
    <footer class="text-center py-4" style="background-color: #343a40; color: #ffffff;">
        <div class="container">
            <p>&copy; 2024 DriveTest Booking. All rights reserved.</p>
            <p>Follow us on 
                <a href="#" class="text-white"><i class="fab fa-facebook-f"></i></a> 
                <a href="#" class="text-white"><i class="fab fa-twitter"></i></a> 
                <a href="#" class="text-white"><i class="fab fa-instagram"></i></a>
            </p>
        </div>
    </footer>
</body>

</html>
